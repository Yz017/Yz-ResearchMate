# ResearchMate · 成本说明与降本方案

> 针对 `research.md` §9 里 ¥500–1000/月 的预算，做一份拆解：每一项钱花在哪、能不能省、省到什么程度。
>
> 结论先说：个人科研助手完全可以做到 **¥0–30/月**（本地开发 + 便宜 LLM API），不必上到 ¥500。原方案的价格是「生产级云部署」，对个人项目过重。

---

## 0. 三档预算方案总览

| 档位 | 月开销 | 形态 | 适合阶段 |
|---|---|---|---|
| **🟢 极省档** | **¥0–10** | 全本地跑（自己电脑 + SQLite + Chroma + CPU embedding）+ DeepSeek API | **M1–M3 开发期，强烈推荐先走这档** |
| **🟡 轻量云档** | **¥30–80** | 一台抢占式 ECS + 同机所有服务 + DeepSeek/Qwen API | 想要 7×24 可用、手机能访问时 |
| **🔴 生产档** | **¥500–1000** | `research.md` 原方案 | 多用户、高并发、业务级 SLA |

个人项目 99% 的情况下 🟢 或 🟡 就够了。下面逐项解释为什么。

---

## 1. 各项开销的用途与替代方案

### 1.1 ACK 节点（¥150/月）· 跑主应用

**干什么的**：运行 ADK agent 主进程 + FastAPI 后端。负责接收用户请求、编排 agent、调用各种工具和 LLM。

**为什么原方案写 ACK**：ACK 是阿里云的 Kubernetes 服务，生产环境有自动扩缩容、滚动发布、故障自愈。这些特性个人项目用不上。

**替代方案（从便宜到贵）**：

| 方案 | 月成本 | 优缺点 |
|---|---|---|
| **本地电脑** | ¥0 | 开发期最佳；关机就没法用；用 `frp`/`ngrok` 可对外暴露 |
| **家用 NAS / 树莓派** | ¥0（一次性硬件） | 7×24，低功耗；需要有硬件 |
| **阿里云学生机**（学生认证） | ¥9.5/月（2c2g） | 学生党首选；有认证门槛 |
| **阿里云/腾讯云新用户活动** | ¥99/年（2c2g） | 首年极便宜，续费贵；可以每年换账号 |
| **抢占式 ECS**（2c4g） | ¥30–50/月 | 比按量便宜 80%；会被强制回收，适合无状态服务 |
| **阿里云函数计算 FC** | 按调用计费，个人量 ¥0–20/月 | 无服务器，不用时零成本；ADK 长连接场景不太适合 |
| **Serverless 容器 SAE** | 按量 ¥50–100/月 | ACK 轻量版，免运维 |
| **ACK 节点**（原方案） | ¥150/月 | 过度设计 ❌ |

**推荐**：🟢 本地电脑 / 🟡 抢占式 ECS 或学生机

---

### 1.2 RDS PostgreSQL（¥80/月）· 会话与元数据

**干什么的**：
- 存 ADK `Session`（对话历史、state），对应短期记忆
- 存论文元数据表（标题、作者、arXiv_id、阅读状态、标签等）

**为什么原方案写 RDS**：托管数据库，自动备份、高可用。个人项目完全用不上高可用。

**替代方案**：

| 方案 | 月成本 | 说明 |
|---|---|---|
| **SQLite 本地文件** | ¥0 | 单用户科研助手完全够用；ADK `DatabaseSessionService` 天然支持 SQLite；定期拷贝到云盘就是备份 |
| **同机 PostgreSQL Docker** | ¥0（复用主机） | 和主应用共享 ECS，一个 docker-compose 拉起来 |
| **Supabase 免费层** | ¥0 | 500MB Postgres + API，个人用绰绰有余 |
| **Neon 免费层** | ¥0 | 0.5GB Serverless PG，冷启动 |
| **RDS PG 基础版** | ¥80/月 | 个人不需要 ❌ |

**推荐**：🟢 SQLite / 🟡 同机 Docker PG（如果真的需要 PG 的 JSON 能力或并发）

---

### 1.3 DashVector（免费额度内）· 向量检索

**干什么的**：
- RAG 检索：文献 chunk 的向量 + 元数据存储
- 长期记忆：记忆片段的语义检索

**实际情况**：原方案已经说"免费额度内"，这项其实 ¥0。但值得评估托管 vs 自建的权衡。

**替代方案**：

| 方案 | 月成本 | 优缺点 |
|---|---|---|
| **Chroma 嵌入式** | ¥0 | 本地 SQLite 后端，10 万向量规模毫无压力；Python 直接 import 就能用 |
| **Milvus Lite** | ¥0 | 也是嵌入式，性能比 Chroma 好 |
| **Qdrant Docker** | ¥0（复用主机） | 独立服务，REST API，功能最全 |
| **PostgreSQL + pgvector** | ¥0（复用 PG） | 和元数据共库，事务一致；规模大时性能不如专用库 |
| **DashVector 免费层** | ¥0 | 阿里云原生，无需运维；vendor lock-in |

**推荐**：🟢 Chroma（一行 pip 装好）/ 🟡 保持 DashVector（反正免费）

---

### 1.4 PAI-EAS（¥50–200/月）· bge-m3 推理 ⚠️ 大头

**干什么的**：把 bge-m3 embedding 模型部署成 HTTP 服务。每次 RAG ingest 或 query 都要调它把文本变成向量。

**为什么原方案写 PAI-EAS**：弹性 GPU，按 QPS 计费。对于个人"偶尔用"的场景，这个钱花得很亏——冷启动 30 秒，热实例空转也要钱。

**这项是最容易省的**，因为 embedding 既可以本地跑也可以用便宜 API：

| 方案 | 月成本 | 性能 | 说明 |
|---|---|---|---|
| **本地 CPU 跑 bge-m3** | ¥0 | 首次加载 10s，单条 embedding ~100ms | M3 Mac / 主流 x86 CPU 都够用；批处理上千 chunk 也就几分钟。个人量完全够。用 `sentence-transformers` 或 `FlagEmbedding` |
| **本地 GPU（如果有显卡）** | ¥0 | 毫秒级 | 有 RTX 3060 级显卡就起飞 |
| **硅基流动 SiliconFlow** | 免费额度 + 按量 | 云端毫秒级 | 提供 bge-m3 托管 API，有免费额度，超出约 ¥0.0001/千字符，个人用量几乎免费 |
| **阿里云 DashScope `text-embedding-v3`** | ¥0.0005/千字符 | 云端毫秒级 | 不是 bge-m3，是通义自家模型，中英效果也不错；个人月度预算 ¥1–5 |
| **Jina AI embeddings 免费层** | 1M token 免费/月 | 云端 | 够个人用 |
| **PAI-EAS 弹性 GPU** | ¥50–200/月 | 毫秒级 | 对个人过度 ❌ |

**推荐**：🟢 本地 CPU 跑 bge-m3（开发期，完全够）/ 🟡 硅基流动免费层（需要云端时）

**数字直觉**：假设每天读 10 篇论文（~3 万字/篇 ≈ 30 万字符），总计 300 万字符/天。
- 本地 CPU：电费 < ¥1/天
- 硅基流动：免费额度通常够
- DashScope：约 ¥1.5/天 ≈ ¥45/月

---

### 1.5 OSS（¥10/月）· 文件存储

**干什么的**：存上传的 PDF 原件、生成的周报 Markdown / docx。

**替代方案**：

| 方案 | 月成本 | 说明 |
|---|---|---|
| **本地磁盘** | ¥0 | 开发期直接放 `./artifacts/`；配合 Syncthing/云盘自动同步就有异地备份 |
| **GitHub 私有仓库** | ¥0 | < 1GB；按目录管理 PDF 其实挺清爽 |
| **OSS 新用户免费层** | ¥0 | 阿里云新用户通常送 40GB 3 个月 |
| **OSS 按量** | ¥10/月 | 价格其实很低，不是省钱重点 |

**推荐**：🟢 本地磁盘 / 🟡 OSS（10 块真的不算贵，方便对接其他阿里云服务）

---

### 1.6 Azure OpenAI GPT（¥200–500/月）· LLM 调用 ⚠️ 大头

**干什么的**：所有 agent 的"大脑"。Coordinator 决策、Librarian 摘要、Writer 生成周报都靠它。

**这是第二个大头，也是最容易省的**，因为：
1. 国产模型已经便宜到接近免费
2. GPT 本身也有 mini 款，比旗舰款便宜 10 倍
3. 命中 prompt cache 后输入成本再降 50–90%

**价格对比**（按「输入 + 输出 1:1 混合，100 万 token」估算）：

| 模型 | 输入（/M token） | 输出（/M token） | 100 万 token 综合成本 | 相对价格 |
|---|---|---|---|---|
| GPT-4.1（Azure） | ~¥14 | ~¥56 | ~¥35 | 1.0× |
| GPT-4.1 mini | ~¥3 | ~¥11 | ~¥7 | **0.2×** |
| GPT-4o mini | ~¥1 | ~¥4 | ~¥2.5 | **0.07×** |
| Claude Sonnet 4.6 | ~¥22 | ~¥108 | ~¥65 | 1.9× |
| **DeepSeek-V3** | **~¥2** | **~¥8** | **~¥5** | **0.14×** |
| **通义 Qwen3-Max** | ~¥4 | ~¥16 | ~¥10 | 0.28× |
| 智谱 GLM-4.5 | ~¥2 | ~¥8 | ~¥5 | 0.14× |
| Ollama 本地（Qwen3-8B） | ¥0 | ¥0 | ¥0（但慢 + 质量下降） | 0× |

**个人月度用量估算**：
- 每天 20 次对话 × 每次 5k token（含 RAG 上下文） × 30 天 = 300 万 token/月
- 用 GPT-4.1：¥100/月
- 用 GPT-4o mini：¥7.5/月
- 用 **DeepSeek-V3：¥15/月**
- 用 Qwen3-Max：¥30/月

**替代策略**：

| 策略 | 说明 |
|---|---|
| **分层用模型** | Coordinator / Writer 用好模型（DeepSeek-V3 或 GPT-4.1），Scout 这种调工具的用 mini（GPT-4o mini / Qwen-Turbo） |
| **走 OpenRouter** | 一个 API key 接所有模型，省去申请 Azure 的麻烦；支持按模型自动路由 |
| **首选 DeepSeek-V3** | 性价比之王，推理能力接近 GPT-4.1；国内访问稳定 |
| **prompt caching** | 系统提示词稳定，开启缓存后命中率高，输入成本降 50–90% |
| **限额控制** | ADK Callback 里做 per-session token 预算，超额截断 |

**推荐**：
- 🟢 **DeepSeek-V3 做主力**（¥5–15/月）+ 少量 GPT-4.1 兜底复杂任务
- 🟡 OpenRouter 统一接入，按模型自动路由
- 🔴 真要用 GPT 主力才上 Azure OpenAI

---

## 2. 三档方案详细清单

### 🟢 极省档：¥0–10/月

**定位**：开发 + 个人日常使用，不追求 7×24 在线。

| 组件 | 选型 | 成本 |
|---|---|---|
| 主应用 | 本地电脑跑 | ¥0 |
| Session DB | SQLite 文件 | ¥0 |
| 向量库 | Chroma 嵌入式 | ¥0 |
| Embedding | 本地 CPU bge-m3 | ¥0 |
| 文件存储 | 本地 `./data/` + 云盘同步 | ¥0 |
| **LLM** | **DeepSeek-V3 API** | **¥5–15/月** |
| 外部工具 | arXiv 官方 API（免费）+ S2 免费层 | ¥0 |
| **合计** | | **¥5–15/月** |

**远程访问**：用 `cloudflare tunnel`（免费）或 `frp`（自购 ECS 做跳板），手机也能连。

---

### 🟡 轻量云档：¥30–80/月

**定位**：需要 7×24 在线，或者不想让自己电脑一直开机。

| 组件 | 选型 | 成本 |
|---|---|---|
| 主应用 | 抢占式 ECS 2c4g（或学生机） | ¥10–50/月 |
| Session DB | 同机 SQLite | ¥0 |
| 向量库 | 同机 Chroma / Qdrant Docker | ¥0 |
| Embedding | 硅基流动 bge-m3 托管（免费额度）或同机 CPU | ¥0–5 |
| 文件存储 | OSS 低频访问 | ¥5–10 |
| **LLM** | DeepSeek-V3 或 Qwen3-Max | ¥15–30/月 |
| 外部工具 | 同上 + 按需开 SerpAPI | ¥0 |
| **合计** | | **¥30–80/月** |

**备份**：SQLite + Chroma 数据每天 rsync 到 OSS，成本几乎为 0。

---

### 🔴 生产档：¥500–1000/月

见 `research.md` §9，适合做成产品给多人用时再考虑。个人项目别上。

---

## 3. 建议的落地节奏

**M1–M3（前 4–6 周）：只花 LLM 的钱**
- 本地跑一切，只用 DeepSeek API（实名认证后充 ¥50 能用两三个月）
- 不买任何云资源，专注把功能跑通

**M4（上线）：按需上云**
- 功能稳定了再评估：自己电脑够不够用、有没有手机访问需求
- 如果需要远程访问 → 上 🟡 档
- 如果就自己电脑用 → 继续 🟢 档，加个 Cloudflare Tunnel 也能远程

**什么时候才需要 🔴 档**：
- 你把这个项目开放给实验室 5 个以上同学一起用
- 或者做成 SaaS 对外付费
- 在那之前，¥500/月 纯属浪费

---

## 4. 省钱陷阱与注意

- **新用户优惠是一次性的**：阿里云/腾讯云新用户 ¥99/年很香，但续费就原价了。可以每年换一家云厂商薅一次。
- **抢占式实例会被回收**：一般 1–24 小时无征兆终止。用它跑的服务必须**无状态**，数据全写 OSS / 托管 DB。ResearchMate 本身是无状态的（state 全在 SQLite/OSS），配合就行。
- **DeepSeek / 国产 LLM 的合规**：数据会经过国内厂商的服务器。个人科研笔记一般没敏感性，但如果涉及未发表 idea，自己掂量。
- **prompt injection 攻击成本**：和模型价格无关，用便宜模型不代表能省掉安全加固（`<external_content>` 标签 + system prompt 禁令还是要做）。
- **token 消耗会失控**：ADK 多 agent 架构每一轮对话可能跑 3–5 个 LLM 调用。一定要在 Coordinator 的 callback 里加 **per-session token 预算**（比如单次对话 50k token 上限），否则出 bug 时可能一夜烧几十块。

---

## 5. 最终推荐

**对当前这个项目**（个人科研助手，你一个人用）：

> **M1–M4 全程走极省档：本地 + SQLite + Chroma + 本地 bge-m3 + DeepSeek-V3**
> **月成本 ¥5–15，一杯奶茶钱**

原方案的 ¥500–1000/月 是按「生产级部署」预留的，个人用纯属浪费。等真有需要再平滑升级——所有组件都能从本地无缝迁到云端对应服务（SQLite → RDS / Chroma → DashVector / 本地 embedding → PAI-EAS / DeepSeek → GPT），研究文档里的架构设计本身是不变的，只是后端替换。
